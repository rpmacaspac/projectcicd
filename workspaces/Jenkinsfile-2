

pipeline {
    agent {
        docker { image 'woozymasta/kaniko-tool'}
    }

    stages {
        stage('Test'){
            steps {
                sh 'docker --version'
            }
        }
    }
}