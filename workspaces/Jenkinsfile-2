pipeline {
    agent {
        kubernetes {
            inheritFrom 'dind-agent'
            defaultContainer 'dind-container'
        }
    }
    stages {
        stage('Build') {
            steps {
                container('dind-container') {
                    sh 'docker version'
                    sh 'docker info'
                }
            }
        }
    }
}
